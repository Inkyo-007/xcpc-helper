"""业务模块层：每个功能一个子目录，包含该功能的领域核心。

【初学者导读】
modules/ 是后端的"业务大脑"所在地。
一个功能一个子目录，比如 template/ 就是"模板库"功能的全部核心逻辑：
扫描目录 -> 解析 README -> 建立检索索引 -> 监听变更。
上层的 services/ 负责组织调用这些模块能力，
routers/ 则负责把结果暴露成 HTTP 接口。
"""
