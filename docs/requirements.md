# xcpc-helper 项目需求

> 本文档维护项目的功能清单（含优先级）与非功能需求。
> 各功能的设计与实现细节见 [design/](design/README.md)。

## 1. 功能清单

### 模板整理及生成

**模板库**（已实现，设计见 [design/template/template-library.md](design/template/template-library.md)）

- 模板的显示页面：标题、分类、标签（多选）、代码、语言、说明（markdown）、复杂度、来源题目链接；
- 代码编辑器：语法高亮、行号、只读预览模式；
- 一键复制代码到剪贴板；
- 同一模板下的多语言/多变体管理（一个模板卡片下挂多个代码版本）；
- 可视化增删改：新增模板、新建版本、编辑元数据与代码、删除版本/模板；
- 主标签重命名与换分类（后端接口已就绪）；
- 左侧分类 + 标签过滤 + 关键词搜索（标题/说明/代码全文）；
- 模板列表支持按更新时间/名称/自定义优先级排序。

**打印册**（已实现，设计见 [design/template/printbook.md](design/template/printbook.md)）

- 从模板库中按版本勾选模板，与章节标题、自由文字、图片、显式分页符组合为有序块序列，生成单本"打印册"，支持拖拽排序与指定位置插入；
- 导出 PDF：固定版式封面、带页码的层级目录、每模板一节（标题 + 元信息 + 代码块 + 可选说明框）、中文字体与公式支持；
- 打印册模板化：保存多套打印册配置（如"ICPC 区域赛版"、"校内赛版"），可重复导出。

**导入 / 导出**（已实现，设计见 [design/template/transfer.md](design/template/transfer.md)）

- 模板库与打印册分开导入/导出（zip 压缩包），用于备份与跨机器迁移；
- 导出即规范化：无论当前目录形态如何，统一整理为"分类 / 模板 / 版本"三层标准结构，代码统一 UTF-8 编码；
- 导入最大程度适配选手自收集的模板库（分类文件夹 + 平铺代码文件，每份代码即一份模板），不规范内容列入警告清单，可选择继续导入可识别部分；
- 导入策略：合并导入（跳过 / 自动重命名同名项）或整体替代（清空现有库后导入，带不可撤销警示）。

**模板整理的后续方向（未排期）**

- 全局快捷键唤起搜索框（类似 Spotlight 的速查面板）；
- 界面主题、打印样式自定义（字体、字号、每页列数、是否显示行号）。

### 训练统计（activity）

**数据聚合**（已实现，设计见 [design/activity/conventions.md](design/activity/conventions.md)）

- 多平台训练数据整合：支持 Codeforces、AtCoder、洛谷、牛客竞赛；
- 用户组管理：支持多用户组隔离，组内绑定各平台账号，组间数据完全独立；
- 信息卡：每组独立设置 ID、签名、头像，与组名分离；
- 自动同步：应用启动时自动触发增量同步，支持手动触发全量同步；
- 增量同步与断点续传：按游标水位增量拉取，中断后可从断点续传，不丢数据；
- 凭据安全：cookie 平台（洛谷）的凭据本地加密存储，支持一键登录或手动输入，支持凭据过期重新授权。

**数据展示**（已实现）

- 统计卡片：总解题数、总提交数、今日解题、连续活跃天数；
- Activity 热力图：GitHub 式近一年热力图，支持悬浮提示与点击选中联动明细；
- 近期提交：跨平台合并的最后 200 条提交，支持分页、平台筛选与日期明细切换；
- 柱状图：近 7 天通过 / 近 12 个月通过趋势，ECharts 渲染；

**平台适配状态**

| 平台 | 状态 | 接入方式 | 备注 |
|------|------|---------|------|
| Codeforces | 已实现 | 官方 API，匿名 | 第一期，风险最低 |
| AtCoder | 已实现 | kenkoooo 第三方 API | 第二期，匿名可取 |
| 洛谷 | 已实现 | cookie 授权 + 反爬对抗 | 第三期，首个凭据平台 |
| 牛客竞赛 | 已实现 | HTML 解析 + 时区转换 | 第四期 |
| LeetCode CN | 规划中 | GraphQL（路径已探明） | 待排期 |
| 更多平台 | 待评估 | — | — |

**训练统计的后续方向（未排期）**

- LeetCode 国际站 / 中国站适配
- 平台 rating 折线图引入

### 在线比赛信息

- 近期比赛列表聚合（多平台）
- 比赛提醒功能

> 状态：需求已明确，待设计排期。平台接口调研已完成，详见 [docs/cache/platform-api-research.md](docs/cache/platform-api-research.md)。

## 2. 非功能需求

- **本地部署**：单机运行，离线可用，无需注册登录；默认单用户。
- **轻量**：选手的笔记本环境各异，依赖越少越好；启动命令一行搞定（或绿色可执行包）。
- **响应快**：本地应用，检索和复制必须是毫秒级。
- **可移植**：Windows 优先（竞赛选手主流），兼顾 macOS/Linux。
